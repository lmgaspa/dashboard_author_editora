import { Routes } from '@angular/router';
import { DashboardPage } from '@/app/features/dashboard/pages/home/dashboard-page';

export const routes: Routes = [
  { path: '', component: DashboardPage },
];
